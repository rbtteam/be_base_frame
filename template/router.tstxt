'use strict';
import { Application } from 'egg';
/**
 * @param {Egg.Application} app - egg application
 */
 export default (app: Application) => {
    const { router, controller, middleware } = app;
    const { requestValidate, roleValidate } = middleware;
    [List]
}